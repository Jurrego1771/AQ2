{
  "name": "aq2-automation",
  "version": "1.0.0",
  "description": "Playwright + JS test automation",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "smoke": "playwright test --project=setup --project=smoke",
    "api": "playwright test --project=api",
    "e2e": "playwright test --project=setup --project=e2e",
    "regression": "playwright test --project=setup --project=regression",
    "report": "playwright show-report",
    "codegen": "playwright codegen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^10.5.0",
    "@playwright/test": "^1.61.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  }
}
