version: 1
modulo: "category"
ultima_actualizacion: '2026-07-08'

tests:
  - id: "CAT-TC-001"
    titulo: "GET /api/category devuelve List Response valido contra Zod"
    modulo: "category"
    tipo: "contract"
    prioridad: "high"
    estado: "active"
    refs:
      validates: ["CAT-AC-1"]
      mitigates: ["CAT-RISK-1", "CAT-RISK-3"]
      story: "US-035"
      spec: "tests/api/contract/category.api.contract.spec.js"
    # Migrado de api_test_flow/tests/api/smoke/category/category-crud.smoke.spec.js.
    # Verifica que el shape envelope jsonp {status:'OK', data:[...]} se mantiene
    # y que cada item tiene al menos _id.

  - id: "CAT-TC-002"
    titulo: "GET /api/category sin auth devuelve 403 INVALID_TOKEN (no 500)"
    modulo: "category"
    tipo: "contract"
    prioridad: "high"
    estado: "active"
    refs:
      validates: ["CAT-AC-1"]
      mitigates: ["CAT-RISK-2"]
      story: "US-035"
      spec: "tests/api/contract/category.api.contract.spec.js"
    # Cobertura explicita del negativo: distingue 403 INVALID_TOKEN de un 5xx
    # que indicaria fuga de error.

  - id: "CAT-TC-003"
    titulo: "GET /api/category/:id devuelve Get Response valido contra Zod"
    modulo: "category"
    tipo: "contract"
    prioridad: "high"
    estado: "active"
    refs:
      validates: ["CAT-AC-1"]
      mitigates: []
      story: "US-035"
      spec: "tests/api/contract/category.api.contract.spec.js"
    # Tomamos el _id del primer item del listado, asi evitamos hardcodear un
    # id que podria no existir en otras cuentas.

  - id: "CAT-TC-004"
    titulo: "GET /api/category/:fake_id devuelve 404 NOT_FOUND"
    modulo: "category"
    tipo: "contract"
    prioridad: "medium"
    estado: "active"
    refs:
      validates: ["CAT-AC-1"]
      mitigates: []
      story: "US-035"
      spec: "tests/api/contract/category.api.contract.spec.js"
